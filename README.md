# extractSSA
A Bash script to extract all embedded ASS/SSA subtitles from video files as properly-named external plain SubRip files.

It relies on ffmpeg and sed and should work on any Linux distribution, *BSD, and macOS.
